@model HDKTech.Models.SanPham
@using HDKTech.Utils
@{
    ViewData["Title"] = Model.TenSanPham + " - HDKTech";

    var defaultImg = Model.HinhAnhs?.FirstOrDefault(h => h.IsDefault)?.Url 
                     ?? Model.HinhAnhs?.FirstOrDefault()?.Url 
                     ?? "no-image.png";
}

<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/glightbox/3.2.0/css/glightbox.min.css">
<link rel="stylesheet" href="~/css/pages/product-details.css" />

<div class="container py-4">
    <div class="details-body">
        <div class="row g-4">
            <div class="col-lg-7">
                <div class="main-img-container">
                    <a href="@ImageHelper.GetImagePath(defaultImg, Model.DanhMuc?.TenDanhMuc)" class="glightbox">
                        <img id="mainImg" src="@ImageHelper.GetImagePath(defaultImg, Model.DanhMuc?.TenDanhMuc)" class="main-img" onerror="handleImageError(this)">
                    </a>
                </div>
                <p class="text-center text-muted small mt-2"><i class="bi bi-search"></i> Click vào ảnh để phóng to</p>
                @if (Model.HinhAnhs != null && Model.HinhAnhs.Any())
                {
                    <div class="thumb-container">
                        @foreach (var img in Model.HinhAnhs)
                        {
                            <img src="@ImageHelper.GetImagePath(img.Url, Model.DanhMuc?.TenDanhMuc)" class="thumb-item @(img.IsDefault ? "active" : "")" 
                                 onclick="updateMainImage('@ImageHelper.GetImagePath(img.Url, Model.DanhMuc?.TenDanhMuc)', this)" onerror="handleImageError(this)">
                        }
                    </div>
                }

            </div>

            <div class="col-lg-5">
                <h2 class="h4 fw-bold mb-1">@Model.TenSanPham</h2>
                <div class="text-muted small mb-2">Thương hiệu: <strong>@Model.HangSX?.TenHangSX</strong> | SKU: <strong>HDK-@Model.MaSanPham</strong></div>

                @{
                    var discountPercent = Model.GiaNiemYet > 0 
                        ? (int)((Model.GiaNiemYet - Model.Gia) / Model.GiaNiemYet * 100) 
                        : 0;
                }

                <div class="d-flex align-items-center gap-2 mb-3">
                    <div>
                        <span class="price-text">@Model.Gia.ToString("N0")₫</span>
                        @if(Model.GiaNiemYet > Model.Gia){
                            <span class="price-old">@Model.GiaNiemYet?.ToString("N0")₫</span>
                        }
                    </div>
                    @if(discountPercent > 0){
                        <span style="background: #fbbf24; color: #000; padding: 6px 12px; border-radius: 6px; font-weight: 700; font-size: 0.9rem; white-space: nowrap;">
                            <i class="bi bi-percent"></i> GIẢM @discountPercent%
                        </span>
                    }
                </div>

                <div class="d-flex gap-2 mb-3">
                    <div class="input-group" style="width: 110px;">
                        <button class="btn btn-outline-secondary btn-sm" onclick="updateQty(-1)">-</button>
                        <input type="text" id="qtyInput" class="form-control text-center fw-bold" value="1" readonly style="font-size: 0.9rem;">
                        <button class="btn btn-outline-secondary btn-sm" onclick="updateQty(1)">+</button>
                    </div>
                    <button class="btn btn-danger flex-grow-1 fw-bold" style="padding: 0.5rem;" onclick="buyNow()">MUA NGAY</button>
                    <button class="btn btn-dark" style="padding: 0.5rem 1rem;" onclick="addToCartAjax(@Model.MaSanPham, '@Model.TenSanPham', document.getElementById('qtyInput').value)">
                        <i class="bi bi-cart-plus"></i>
                    </button>
                </div>

                <div class="promo-red-box">
                    <div class="promo-header"><i class="bi bi-gift"></i> QUÀ TẶNG</div>
                    <div class="promo-content">
                        <div class="promo-item"><i class="bi bi-check-circle"></i> Tặng túi chống sốc HDK Gaming</div>
                        <div class="promo-item"><i class="bi bi-check-circle"></i> Giảm 200k cho sinh viên Bách Khoa</div>
                        <div class="promo-item"><i class="bi bi-check-circle"></i> Trả góp 0% qua thẻ tín dụng</div>
                    </div>
                </div>

                <div class="info-dark-card">
                    <div class="info-dark-item"><i class="bi bi-cpu"></i> <span><strong>Hãng:</strong> @Model.HangSX?.TenHangSX</span></div>
                    <div class="info-dark-item"><i class="bi bi-box"></i> <span><strong>Loại:</strong> @Model.DanhMuc?.TenDanhMuc</span></div>
                    <div class="info-dark-item"><i class="bi bi-shield-check"></i> <span><strong>Bảo hành:</strong> 24 tháng</span></div>
                    <div class="info-dark-item"><i class="bi bi-truck"></i> <span><strong>Vận chuyển:</strong> Toàn quốc</span></div>
                </div>
            </div>
        </div>

        <!-- TABS SECTION - CẤU HÌNH, NHẬN XÉT, MÔ TẢ -->
        <div class="mt-5">
            <ul class="nav nav-tabs nav-tabs-premium" id="productTabs" role="tablist">
                <li class="nav-item" role="presentation">
                    <button class="nav-link active" id="specs-tab" data-bs-toggle="tab" data-bs-target="#specs" type="button" role="tab">
                        <i class="bi bi-diagram-3"></i> CẤU HÌNH
                    </button>
                </li>
                <li class="nav-item" role="presentation">
                    <button class="nav-link" id="reviews-tab" data-bs-toggle="tab" data-bs-target="#reviews" type="button" role="tab">
                        <i class="bi bi-star"></i> NHẬN XÉT (@(Model.DanhGias?.Count() ?? 0))
                    </button>
                </li>
                <li class="nav-item" role="presentation">
                    <button class="nav-link" id="desc-tab" data-bs-toggle="tab" data-bs-target="#desc" type="button" role="tab">
                        <i class="bi bi-file-text"></i> MÔ TẢ
                    </button>
                </li>
            </ul>

            <div class="tab-content border border-top-0 rounded-bottom">
                <!-- TAB 1: CẤU HÌNH -->
                <div class="tab-pane fade show active" id="specs" role="tabpanel">
                    <div class="specs-section" style="padding: 30px; background: #f8f9fa;">
                        @{
                            var specs = Model?.ThongSoKyThuat?.Trim();
                            var hasSpecs = !string.IsNullOrWhiteSpace(specs);
                            var specsArray = hasSpecs ? specs.Split(new[] { '|', ',' }, StringSplitOptions.RemoveEmptyEntries) : new string[] { };
                        }

                        <!-- THÔNG TIN CHUNG -->
                        <div class="specs-category">
                            <div class="specs-category-header">
                                <i class="bi bi-box"></i> THÔNG TIN CHUNG
                            </div>
                            <div class="specs-grid">
                                <div class="spec-item">
                                    <span class="spec-label">Hãng sản xuất</span>
                                    <span class="spec-value">@(Model.HangSX?.TenHangSX ?? "N/A")</span>
                                </div>
                                <div class="spec-item">
                                    <span class="spec-label">Model</span>
                                    <span class="spec-value">@Model.TenSanPham</span>
                                </div>
                                <div class="spec-item">
                                    <span class="spec-label">Danh mục</span>
                                    <span class="spec-value">@(Model.DanhMuc?.TenDanhMuc ?? "N/A")</span>
                                </div>
                                <div class="spec-item">
                                    <span class="spec-label">Bảo hành</span>
                                    <span class="spec-value highlight">@(Model.ThongTinBaoHanh ?? "24 Tháng")</span>
                                </div>
                            </div>
                        </div>

                        <!-- THÔNG SỐ KỸ THUẬT CHI TIẾT -->
                        @if (hasSpecs && specsArray.Length > 0)
                        {
                            <div class="specs-category">
                                <div class="specs-category-header">
                                    <i class="bi bi-diagram-3"></i> THÔNG SỐ KỸ THUẬT CHI TIẾT
                                </div>
                                <div class="specs-grid">
                                    @{
                                        var displayCount = 0;
                                        foreach (var s in specsArray)
                                        {
                                            if (displayCount >= 8) break;
                                            var value = s.Trim();
                                            if (!string.IsNullOrEmpty(value))
                                            {
                                                var parts = value.Split(':');
                                                var label = parts.Length > 0 ? parts[0].Trim() : "Spec";
                                                var specValue = parts.Length > 1 ? parts[1].Trim() : "N/A";

                                                <div class="spec-item">
                                                    <span class="spec-label">@label</span>
                                                    <span class="spec-value">@specValue</span>
                                                </div>
                                                displayCount++;
                                            }
                                        }
                                    }
                                </div>
                            </div>
                        }
                        else
                        {
                            <div class="specs-category">
                                <div class="specs-category-header">
                                    <i class="bi bi-diagram-3"></i> THÔNG SỐ KỸ THUẬT CHI TIẾT
                                </div>
                                <div style="text-align: center; padding: 40px 0; color: #aaa;">
                                    <i class="bi bi-info-circle" style="font-size: 2.5rem; display: block; margin-bottom: 15px;"></i>
                                    <p>Thông tin cấu hình sản phẩm sẽ được cập nhật sớm</p>
                                </div>
                            </div>
                        }
                    </div>
                </div>

                <!-- TAB 2: NHẬN XÉT -->
                <div class="tab-pane fade" id="reviews" role="tabpanel">
                    <div class="reviews-section" style="padding: 30px; background: #f8f9fa;">
                        <!-- RATING SUMMARY -->
                        <div class="rating-summary">
                            <div class="rating-avg">
                                <div class="rating-avg-number">
                                    @{
                                        var avgRating = Model.DanhGias?.Any() == true ? Model.DanhGias.Average(x => x.SoSao) : 0;
                                    }
                                    @avgRating.ToString("0.0")
                                </div>
                                <div class="rating-stars">
                                    @{
                                        var fullStars = (int)avgRating;
                                        for (int i = 0; i < fullStars; i++) { <span>⭐</span> }
                                        for (int i = fullStars; i < 5; i++) { <span>☆</span> }
                                    }
                                </div>
                                <div class="rating-count">Dựa trên @(Model.DanhGias?.Count() ?? 0) đánh giá</div>
                            </div>

                            <div class="rating-breakdown">
                                @for (int i = 5; i >= 1; i--)
                                {
                                    var count = Model.DanhGias?.Count(x => x.SoSao == i) ?? 0;
                                    var percent = (Model.DanhGias?.Count() ?? 0) > 0 ? 
                                        (int)((count * 100.0) / (Model.DanhGias?.Count() ?? 1)) : 0;

                                    <div class="rating-row">
                                        <div class="rating-row-label">
                                            @(new string('★', i))@(new string('☆', 5-i))
                                        </div>
                                        <div class="rating-row-bar">
                                            <div class="rating-row-fill" style="width: @percent%"></div>
                                        </div>
                                        <div class="rating-row-percent">@percent%</div>
                                    </div>
                                }
                            </div>
                        </div>

                        <!-- REVIEWS LIST -->
                        @if (Model.DanhGias?.Any() == true)
                        {
                            <div class="reviews-list">
                                @foreach (var review in Model.DanhGias.OrderByDescending(x => x.NgayDanhGia).Take(5))
                                {
                                    <div class="review-item">
                                        <div class="review-header">
                                            <div>
                                                <div class="review-author">@(review.NguoiDung?.HoTen ?? "Ẩn danh")</div>
                                                <div class="review-meta">
                                                    <span class="review-verified">✓ Xác thực mua hàng</span>
                                                    <span>@review.NgayDanhGia.ToString("d MMMM yyyy", new System.Globalization.CultureInfo("vi-VN"))</span>
                                                </div>
                                            </div>
                                            <div class="review-rating">
                                                @(new string('★', review.SoSao))@(new string('☆', 5 - review.SoSao))
                                            </div>
                                        </div>

                                        <div class="review-title">@(review.NoiDung?.Split('.').FirstOrDefault() ?? "Nhận xét")</div>
                                        <div class="review-content">@review.NoiDung</div>

                                        <div class="review-actions">
                                            <button class="review-action-btn" type="button" onclick="toggleLike(this)">
                                                👍 <span>0</span>
                                            </button>
                                            <button class="review-action-btn" type="button" onclick="toggleDislike(this)">
                                                👎 <span>0</span>
                                            </button>
                                        </div>
                                    </div>
                                }
                            </div>

                            @if ((Model.DanhGias?.Count() ?? 0) > 5)
                            {
                                <button class="load-more-btn" onclick="loadMoreReviews()">TẢI THÊM NHẬN XÉT</button>
                            }
                        }
                        else
                        {
                            <div style="text-align: center; padding: 40px; background: white; border-radius: 8px;">
                                <i class="bi bi-chat-left" style="font-size: 2.5rem; color: #ccc; display: block; margin-bottom: 15px;"></i>
                                <p style="color: #999; margin: 0;">Chưa có đánh giá nào. Hãy là người đầu tiên đánh giá sản phẩm này!</p>
                            </div>
                        }
                    </div>
                </div>

                <!-- TAB 3: MÔ TẢ -->
                <div class="tab-pane fade" id="desc" role="tabpanel">
                    <div class="description-section" style="padding: 30px; background: white;">
                        <h2 class="section-title" style="font-size: 20px; font-weight: 700; color: #1f2937; margin-bottom: 20px; padding-bottom: 15px; border-bottom: 3px solid #ef4444;">
                            <i class="bi bi-file-text"></i> Mô tả sản phẩm
                        </h2>

                        <div class="description-content" style="color: #6b7280; line-height: 1.8; font-size: 15px;">
                            @Html.Raw(Model.MoTaSanPham ?? "<p style='color: #aaa;'>Chưa có mô tả sản phẩm</p>")
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="related-products-section">
            <h4 class="related-section-title"><i class="bi bi-fire"></i> SẢN PHẨM LIÊN QUAN</h4>
            <div class="related-products-grid">
                @if (ViewBag.RelatedProducts != null)
                {
                    @foreach (var item in (List<HDKTech.Models.SanPham>)ViewBag.RelatedProducts)
                    {
                        var fileName = item.HinhAnhs?.FirstOrDefault(h => h.IsDefault)?.Url
                                       ?? item.HinhAnhs?.FirstOrDefault()?.Url
                                       ?? "no-image.png";
                        var relatedImagePath = "/images/products/" + fileName;

                        <div class="related-card-wrapper">
                            <div class="related-card">
                                <div class="related-image-container">
                                    <a href="/Product/Details/@item.MaSanPham" class="related-image-link">
                                        <img src="@relatedImagePath" class="related-img" alt="@item.TenSanPham" onerror="this.onerror=null; this.src='/images/products/no-image.png';">
                                    </a>
                                </div>
                                <div class="related-card-body">
                                    <div class="related-title">@item.TenSanPham</div>
                                    <div class="related-price">@item.Gia.ToString("N0")₫</div>
                                    <a href="/Product/Details/@item.MaSanPham" class="btn-view">Xem ngay</a>
                                </div>
                            </div>
                        </div>
                    }
                }
            </div>
        </div>
    </div>
</div>

<script src="https://cdnjs.cloudflare.com/ajax/libs/glightbox/3.2.0/js/glightbox.min.js"></script>
<script>
    const lightbox = GLightbox({ selector: '.glightbox' });
</script>
<script src="~/js/pages/product-details.js"></script>